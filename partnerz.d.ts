interface ParentElement {
  children?: JSX;
}

interface PartnerService {
  head: string;
  pic: string;
  desc: string;
}

interface OverlayProp {
  display: boolean;
}

interface FComment {
  text: string;
  pic: string;
  commentor: string;
}
